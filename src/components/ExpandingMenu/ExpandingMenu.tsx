import * as React from 'react';
import { ExpandingMenuProps } from './types';

function ExpandingMenu(props: ExpandingMenuProps) {
  const { items, width = 300 } = props;
  return <div />;
}

export default ExpandingMenu;
